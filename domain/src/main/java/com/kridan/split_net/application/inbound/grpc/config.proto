syntax = "proto3";

package chat;

service ChatService {
  rpc Chat(stream Envelope) returns (stream Envelope);
}

message Envelope {
  string from = 1;
  string jwt = 2;

  oneof payload {
    Gateways gateways = 3;
  }
}

message Gateways {
  string text = 1;
}
